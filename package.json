{
  "name": "electron-react",
  "version": "1.0.0",
  "description": "hello world!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd src && cross-env BROWSER=none npm start\" \"wait-on tcp:3000 && electron .\"",
    "build": "cd src && npm run build && cd.. && electron-builder"
  },
  "author": "Giovanni",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.2.0",
    "cross-env": "^7.0.3",
    "electron": "^18.2.3",
    "electron-builder": "^23.0.3",
    "wait-on": "^6.0.1"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0"
  }
}
